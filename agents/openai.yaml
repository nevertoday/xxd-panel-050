interface:
  display_name: "/xxd-panel-050"
  short_description: "Create Panel 050 from its original style brief."
  default_prompt: "Use $xxd-panel-050; keep the body of its bundled original style brief as the sole aesthetic authority, send exactly one mode-specific delivery contract per asset, make design-only and wallpaper outputs fully transformed, and ground factual-looking copy in supplied or verified facts."

policy:
  allow_implicit_invocation: true
