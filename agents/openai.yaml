interface:
  display_name: "🦁 XXD Panel 050"
  short_description: "Create source-aware XXD Panel 050 artwork"
  default_prompt: "Use $xxd-panel-050 with my photograph, ask for modes and copy settings, then create fresh raster outputs."
policy:
  allow_implicit_invocation: true
